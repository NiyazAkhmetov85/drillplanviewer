-----

# ⛏️ Drilling Passport Viewer (ЛСК)

## Обзор проекта

Это веб-приложение, созданное на **React (Vite)**, для просмотра, фильтрации и анализа данных паспортов бурения.

Ключевой особенностью является **автоматический пересчет** исходных (сырых) координат (`RawStartPointX/Y`) в Локальную Систему Координат (ЛСК) непосредственно в браузере, согласно предоставленным параметрам трансформации Гельмерта.

Приложение полностью адаптировано для разработки и деплоя через веб-интерфейсы (**GitHub Codespaces** и **Vercel**), не требуя установки локального ПО.

## 🚀 Технологический стек

  * **Фронтенд:** React 18 (Vite)
  * **Язык:** JavaScript
  * **Хостинг/Деплой:** Vercel (Автоматический деплой)
  * **Среда разработки:** GitHub Codespaces
  * **Данные:** JSON-файл, консолидированный из CSV-файлов паспортов.

## 📐 Трансформация Локальной СК (ЛСК)

Трансформация реализована в файле `src/utils/geo.js` как 2D-преобразование Гельмерта (Сдвиг + Поворот).

Параметры, использованные для перевода из USLOVWGS (исходная) в Целевую ЛСК (Система В):

| Параметр | Обозначение | Значение | Источник |
| :--- | :--- | :--- | :--- |
| **Сдвиг по X** ($T_x$) | `TX` | `4458.9140` м | `AltynTau_coord_sys_report.pdf` |
| **Сдвиг по Y** ($T_y$) | `TY` | `7317.3475` м | `AltynTau_coord_sys_report.pdf` |
| **Поворот** ($R$) | `ROT_GON` | `398.9098` гон | `AltynTau_coord_sys_report.pdf` |

**Функционал пересчета включает:**

1.  Парсинг данных: Замена десятичного разделителя (`,` на `.`) для корректного выполнения расчетов.
2.  Применение формулы поворота и сдвига к полям `RawStartPointX/Y` и `RawEndPointX/Y`.

## ⚙️ Начало работы и разработка (Codespaces)

Поскольку проект предназначен для работы в веб-интерфейсе, рекомендуемый способ запуска и модификации — использование **GitHub Codespaces**.

1.  **Открыть Codespace:** В репозитории на GitHub нажмите кнопку `Code` и выберите вкладку `Codespaces`.
2.  **Установка зависимостей:** В терминале Codespace перейдите в корневую папку приложения и выполните:
    ```bash
    npm install
    ```
3.  **Запуск в режиме разработки:**
    ```bash
    npm run dev
    ```
4.  Codespaces предоставит ссылку для открытия приложения в браузере.

## 💾 Структура данных

Приложение ожидает **консолидированный JSON-файл** со всеми данными бурения.

  * **Файл:** `public/data.json`
  * **Требования к полям:** Файл должен содержать (как минимум) столбцы исходных координат:
      * `RawStartPointX`
      * `RawStartPointY`
      * `RawStartPointZ` (для переноса)
      * `RawEndPointX/Y/Z`
      * `HoleName` (для фильтрации)

### Важно:

Перед началом работы необходимо **вручную консолидировать** все отдельные CSV-файлы (`Блока октябрь_...csv`) в единый `data.json` и загрузить его в папку `public/` в репозитории.

## ☁️ Деплой (Vercel)

Проект настроен для **автоматического деплоя** с использованием Vercel:

  * Репозиторий подключен к Vercel.
  * Каждый `git push` в основную ветку (`main`) автоматически запускает сборку и обновление продакшн-версии приложения.
  * Адрес приложения доступен по ссылке Vercel.
